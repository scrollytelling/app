config: &config
  nginx:
    cors: true
staging:
  rails:
    configuration:
      <<: *config
    server:
      unique_name: app
  mysql:
    server:
      unique_name: app
  redis:
    server:
      unique_name: app
production:
  rails:
    configuration:
      <<: *config
    server:
      unique_name: app
  mysql:
    server:
      unique_name: db
  redis:
    server:
      unique_name: cache
